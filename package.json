{
  "name": "tutoringapp-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package.json for monorepo - frontend is in tutoring-platform/",
  "scripts": {
    "install": "cd tutoring-platform && npm install",
    "build": "cd tutoring-platform && npm run build",
    "start": "cd tutoring-platform && npm start"
  }
}

